name: holas

on:
  push:
    branches:
      - main

jobs:
  hola:
    runs-on: windows-latest
    steps:
      - name: hola
        run: echo "holas"
  others:
    runs-on: ubuntu-latest
    needs: [hola]
    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.0
      - name: llamar a mi action
        uses: ./.github/actions/my-action